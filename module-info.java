/**
 * PROG2004 A2 Theme Park Management System Module
 * Defines the module name and marks it as an open module (allows reflective access, simplifying IO/reflective operations in the assignment)
 */
open module prog2004.a2.prvms {
    // Declares dependency on the Java base module (implicitly dependent by default, can be explicitly written for readability)
    requires java.base;
}